import requests

class ComplianceChecker:
    def is_allowed(self, url):
        # This method would check robots.txt and other compliance requirements
        # For simplicity, we'll assume all URLs are allowed for now
        return True